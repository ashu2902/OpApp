import 'package:flutter/material.dart';

class SupportAsVolunteer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('Support As Volunteer')),
      ),
    );
  }
}

class SupportAsIntern extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('Support As Intern')),
      ),
    );
  }
}
